/**
 * Volume tracking utilities for withdrawal limits.
 * Currently disabled as per user requirements (no daily limit).
 */

export const DAILY_LIMIT = 0; // Unused
export const calculateDailyOffRampVolume = () => 0; // Unused
